{
  "name": "@pos/mobile",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cd ../web && NEXT_PUBLIC_CAPACITOR=true pnpm build:mobile",
    "sync": "cap sync",
    "ios": "cap open ios",
    "android": "cap open android",
    "add:ios": "cap add ios",
    "add:android": "cap add android",
    "run:ios": "cap run ios",
    "run:android": "cap run android",
    "deploy:ota": "./scripts/deploy-ota.sh"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/app": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0",
    "@capacitor/keyboard": "^7.0.0",
    "@capacitor/splash-screen": "^7.0.0",
    "@capacitor/status-bar": "^7.0.0",
    "@capgo/capacitor-updater": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
