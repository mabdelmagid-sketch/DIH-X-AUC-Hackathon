[phases.setup]
nixPkgs = ["nodejs_20", "nodePackages.pnpm"]

[phases.install]
cmds = ["pnpm install"]

[phases.build]
cmds = ["pnpm --filter @pos/web build"]

[start]
cmd = "cd apps/web && npx next start -p ${PORT:-3000}"
